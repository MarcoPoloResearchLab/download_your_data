# Changelog

Release entries are generated from the repository history by `make release`.

## Unreleased

- Replace the schema-v1 lifecycle stub with typed schema-v3 Pages, API,
  retained storage, Caddy, health, TAuth, and private-value resources.
- Add deterministic Linux/AMD64 Pages and non-root API container artifacts,
  production-profile parity checks, and Docker smoke validation.
- Restore the zero-argument release, publish, and user-owned deploy entrypoints
  through the required sibling gateway.
